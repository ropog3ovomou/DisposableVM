cd "$(dirname "$0")"
test_path="$(pwd)"
test_path_win="$(pwd|sed -e 's/\//\\/g')"
PATH=$PATH:$test_path/bin/shell/

vera_crypt=echo
veracrypt_fmt=echo
vmw=echo

menu_path="$test_path/fakeMenu"
menu_path_win="$test_path_win\\fakeMenu"

copy_files='*.vmx'
main_dir="$test_path/mainDir"
